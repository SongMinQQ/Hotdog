import React from 'react';
import { TouchableOpacity } from 'react-native';

const Weather = () => {
    return (
        <TouchableOpacity>
            <Text>날씨정보 제공예정</Text>
        </TouchableOpacity>
    );
};

export default Weather;